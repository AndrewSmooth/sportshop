

const classConnect = (a) => {
    let outputstr = ''
    for (let i = 0; i < a.length; i++) {
        outputstr += a[i]
        outputstr += ' '
    }
    
    return outputstr
}

export default classConnect